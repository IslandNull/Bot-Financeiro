> ARCHIVED: historical context only. Do not use as current implementation authority.

Olá Claude, aqui é o Gemini (Arquiteto).

Acabamos de estabilizar a arquitetura de fórmulas do Google Sheets no nosso projeto V52 (Sintaxe em Inglês + separador ponto-e-vírgula + método setFormula). O monolito `Code.js` (com mais de 1.300 linhas) está 100% funcional.

Sua missão agora é executar a modularização segura deste arquivo.

ANTES DE COMEÇAR, é mandatório que você leia os seguintes arquivos para absorver as regras do projeto e seu papel:
1. `.ai_shared/personas/CLAUDE.md` (Sua persona e diretrizes de estilo/refatoração).
2. `.ai_shared/skills/gas_expert.md` (Regras cruciais sobre o ecossistema Google Apps Script).
3. `.ai_shared/handoffs/claude_modularization.md` (O plano de ação exato para esta tarefa).

O Escopo:
Você vai fatiar o `Code.js` para dentro de uma nova pasta `src/` (criando Main.js, Parser.js, Commands.js, Views.js, Actions.js, Setup.js).

Regras de Ouro:
- NÃO altere a lógica interna de NENHUMA função.
- Lembre-se (via gas_expert) que o Apps Script compartilha o escopo global. Não use `export`/`require`/`module.exports`.
- Mova o `appsscript.json` para dentro de `src/` e atualize o `.clasp.json` (adicionando "rootDir": "src") para que o push funcione corretamente.
- Apague o `Code.js` da raiz após fatiá-lo.

Ao finalizar, execute o comando `npm run push` no terminal para validar se o deploy ocorreu com sucesso no Google Apps Script e me avise. Pode começar a leitura!